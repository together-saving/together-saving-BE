spring:
  datasource:
    url: jdbc:mysql://183.99.247.17:12787/test_db
    username: root
    password: 1111
    driver-class-name: org.mariadb.jdbc.Driver
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

server:
  port: 35731